import { Meteor } from 'meteor/meteor';
import { Commits } from '../imports/api/commits.js';
import { CommitsByAuthor } from '../imports/api/commits.js';

Meteor.startup(() => {
  // code to run on server at startup
});
